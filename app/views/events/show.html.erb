<p>
  <strong>Categoría:</strong>
  <%= @event.category %>
</p>

<p>
  <strong>Descripción:</strong>
  <%= @event.description %>
</p>

<p>
  <strong>Ramo:</strong>
  <%= @course.name %>
</p>

<%=
  if (@event.created_by == current_user.email) || current_user.administrator?
    link_to 'Editar', edit_event_path(@event)
  end
%>

<%= link_to 'Volver', course_events_path(@course) %>

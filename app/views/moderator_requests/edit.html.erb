<h1>Responder a la Solicitud</h1>

<%= form_for [@course, @moderator_request] do |f| %>

  <div class="field">
    <%= f.select :status, ModeratorRequest.statuses.keys, placeholder: 'Acción' %>
  </div>

  <div class="actions">
    <%= f.submit "Enviar respuesta" %>
  </div>

<% end %>

<%= link_to 'Mostrar', course_moderator_request_path(@course, @moderator_request) %> |
<%= link_to 'Volver', course_moderator_requests_path %>
